The production server run on a gunicorn server proxyed by nginx (who also serves static files).
The thing is monitored and started by supervisor daemon who start it by running /run_server
Deployment is made by pushing and pulling to a local GIT repository (managable by gitlab)

Here are the configuration files for supervisor and nginx
Everything else is generated by fabric deployment scripts
